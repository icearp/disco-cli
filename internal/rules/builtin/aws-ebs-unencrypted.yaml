version: 1
rules:
  - id: aws-ebs-unencrypted
    description: EBS volume is not encrypted at rest
    severity: medium
    match:
      provider: aws
      type: aws:ec2:volume
      where:
        - path: "Encrypted"
          op: eq
          value: false
