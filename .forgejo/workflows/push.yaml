on: [push]
jobs:
  test:
    runs-on: ubuntu-go-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - run: |
          go mod tidy -diff
          gofmt -l .
        	golangci-lint run ./...
        	go vet ./...
        	CGO_ENABLED=0 go test ./...

